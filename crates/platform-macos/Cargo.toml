[package]
name = "fokus-platform-macos"
version.workspace = true
edition.workspace = true

[dependencies]
fokus-platform = { path = "../platform" }
tracing = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.10"
