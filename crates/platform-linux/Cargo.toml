[package]
name = "odacla-platform-linux"
version.workspace = true
edition.workspace = true

[dependencies]
odacla-platform = { path = "../platform" }
tracing = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
x11rb = { version = "0.13", features = ["screensaver"] }
