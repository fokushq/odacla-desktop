[package]
name = "fokus-platform-linux"
version.workspace = true
edition.workspace = true

[dependencies]
fokus-platform = { path = "../platform" }
tracing = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
x11rb = { version = "0.13", features = ["screensaver"] }
