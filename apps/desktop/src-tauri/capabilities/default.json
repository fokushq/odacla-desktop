{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window (core defaults include window dragging, needed for the macOS overlay title bar)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
